kind: pipeline
name: default

steps:
- name: build
  image: python:3.6-alpine
  commands:
  - apk update && apk add zip
  - zip -r -9 transaction-1.zip *
  
- name: s3-publish
  image: plugins/s3
  settings:
    access_key: AKIA3B4U6ZRO63IEEKZB
    secret_key: 
      from_secret: p_aws
    region: us-west-2
    bucket: tkharness
    source: transaction-${DRONE_BUILD_NO}.zip
    when:
      event:
      - push
